<%= form_for(@tv) do |f| %>
  <% if @tv.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tv.errors.count, "error") %> prohibited this tv from being saved:</h2>

      <ul>
      <% @tv.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>

   <div class="field">
    <%= f.label :genre_list %><br />
    <%= f.text_field :genre_list %>
  </div>

  <div class="field">
    <%= f.label :story %><br />
    <%= f.text_area :story %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
